
подключить Reactronic
подключить Verstak

выполнить
{
  пусть время = transaction<Clock>({ Clock(100) }, [])

  Page(
    script = 
    {
      пусть с = el.style
      с.color = "#f0f0f0"
      с.background-color = "#1a3043"
      с.font-size = "300%"
      // el.horizontal = растяжка-области

      row-break() // новая-строка()

      Panel(
        // mode = Mode.independent-update,
        creation =
        {
          // el.horizontal выравнивание = растяжка-области
          // el.выравнивание-внутри = центр-области
        },
        script =
        {
          Note("Поехали!")

          row-break()

          Note("{время.hour}:{время.minute}:{время.second}")
        })
    })
}
