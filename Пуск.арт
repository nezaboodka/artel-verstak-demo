
подключить Реактроник
подключить Verstak

выполнить
{
  пусть время = транзакция<Часы>({ Часы(100) }, [])

  Page(
    script = 
    {
      // пусть с = эл.стиль
      // с.цвет = "#f0f0f0"
      // с.цвет-фона = "#1a3043"
      // с.размер-шрифта = "300%"
      // эл.выравнивание = растяжка-области

      row-break() // новая-строка()

      Panel(
        // mode = Mode.independent-update,
        creation =
        {
          // эл.выравнивание = растяжка-области
          // эл.выравнивание-внутри = центр-области
        },
        script =
        {
          Note("Поехали!")

          row-break()

          Note("{время.час}:{время.минута}:{время.секунда}")
        })
    })
}
