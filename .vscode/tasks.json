{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Пуск (vite)",
      "group": "build",
      "type": "shell",
      "command": "npm run пуск",
      "options": {
        "cwd": "${workspaceFolder}/.сборка"
      },
    },
    {
      "label": "Собрать (артель собрать)",
      "group": "build",
      "type": "shell",
      "command": "npm run собрать",
      "options": {
        "cwd": "${workspaceFolder}/.сборка"
      },
    },
    {
      "label": "Скачать пакеты (артель скачать-пакеты)",
      "group": "build",
      "type": "shell",
      "command": "npm run скачать-пакеты",
      "options": {
        "cwd": "${workspaceFolder}/.сборка"
      },
    },
  ]
}
