
подключить Реактроник
подключить Верстак

выполнить
{
  пусть время = транзакция<Часы>({ Часы(100) })

  Страница(
  {
    пусть s = element.native.style
    s.color = "#f0f0f0"
    s.backgroundColor = "#1a3043"
    s.fontSize = "300%"

    Секция(независимое-обновление,
    {
      element.рост-ширины = 1
      element.рост-высоты = 1
      element.выравнивание-элемента = по-границам
      element.выравнивание-содержимого = по-центру

      Надпись("Поехали!")

      начать-новую-строку()
      Надпись("{время.час}:{время.минута}:{время.секунда}")
    })
  })
}
