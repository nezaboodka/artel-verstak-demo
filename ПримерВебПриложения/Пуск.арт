
подключить Реактроник
подключить Верстак

выполнить
{
  пусть время = транзакция<Часы>({ Часы(100) })

  Страница(
  {
    пусть с = element.native.style
    // с.цвет = "#f0f0f0"
    // с.цвет-фона = "#1a3043"
    // с.размер-шрифта = "300%"

    Секция(независимое-обновление,
    {
      element.width-growth = 1
      element.height-growth = 1
      element.element-alignment = по-границам
      element.content-alignment = по-центру

      Надпись("Поехали!")

      начать-новую-строку()
      Надпись("{время.час}:{время.минута}:{время.секунда}")
    })
  })
}
