
подключить Реактроник
подключить Верстак

выполнить
{
  пусть время = транзакция<Часы>({ Часы(100) })

  Страница(
  {
    пусть s = el.native.style
    s.color = "#f0f0f0"
    s.backgroundColor = "#1a3043"
    s.fontSize = "300%"

    Секция(независимое-обновление,
    {
      el.рост-ширины = 1
      el.рост-высоты = 1
      el.выравнивание-элемента = по-границам
      el.выравнивание-содержимого = по-центру

      Надпись("Поехали!")

      начать-новую-строку()
      Надпись("{время.час}:{время.минута}:{время.секунда}")
    })
  })
}
