import artel
import artel.js

type Comparer = operation(a: T, b: T): Integer

external
{
  // SealedArray

  #ВнешняяРеализация(модуль = "reactronic")
  type SealedArray<T> = /*(!) abstract */ object Array<T>, Sealed<Array<T>>
  {
  }
}
