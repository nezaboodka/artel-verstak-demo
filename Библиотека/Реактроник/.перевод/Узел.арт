
псевдонимы(русский)
{
  тип Обработчик<Т>(эл, базис) = Delegate<T>(el, basis)

  тип Режим = Mode
  {
    стандартный = default
    независимое-обновление = independent-update
    ручное-встраивание = manual-mount
  }
}
