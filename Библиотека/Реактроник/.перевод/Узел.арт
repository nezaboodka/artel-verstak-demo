
псевдонимы(русский)
{
  type Delegate<T>(el, basis) = Обработчик<Т>(эл, базис)

  type Mode = Режим
  {
    default = стандартный
    independent-update = независимое-обновление
    manual-mount = ручное-встраивание
  }
}
