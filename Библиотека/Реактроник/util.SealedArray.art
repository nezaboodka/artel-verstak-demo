
import js = Artel.JavaScript

type Comparer<T> = operation(a: T, b: T): Integer

external
{
  // SealedArray

  #js.ВнешняяРеализация(модуль = "reactronic")
  type SealedArray<T> = /*(!) abstract */ object Array<T>, Sealed<Array<T>>
  {
  }
}
