import Artel
import js = Artel.JavaScript

external
{
  // Worker

  #js.ВнешняяРеализация(модуль = "reactronic")
  type Worker = aspect
  {
    protected
    id: Integer

    protected
    hint: Text

    protected
    isCanceled: YesNo

    protected
    isFinished: YesNo

    operation cancel(err: Error, restartAfter: Worker? = empty): Object?

    parallel
    operation whenFinished()
  }
}
