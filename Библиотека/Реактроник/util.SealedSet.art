import Artel
import js = Artel.JavaScript

external
{
  // SealedSet

  #js.ВнешняяРеализация(модуль = "reactronic")
  type SealedSet<T> = /*(!) abstract */ object
    Set<T>, Sealed<Set<T>>
  {
  }
}
