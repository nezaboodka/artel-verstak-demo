import Artel
import js = Artel.JavaScript

external
{
  // SealedMap

  #js.ВнешняяРеализация(модуль = "reactronic")
  type SealedMap<K, V> = /*(!) abstract */ object Map<K, V>, Sealed<Map<K, V>>
  {
  }
}
