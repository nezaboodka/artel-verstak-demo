
import JS = Artel.JavaScript
import Браузер

external
{
  #JS.ВнешняяРеализация(модуль = "verstak")
  operation FocuserReaction(
    key: Text,
    target: HTMLElement,
    model: FocusModel,
    switchEditMode: операция(model: FocusModel?): None? /*(!)= none*/)
}
