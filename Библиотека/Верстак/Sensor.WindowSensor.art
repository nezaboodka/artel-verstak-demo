import artel
import artel.js

external
{
  #ВнешняяРеализация(модуль = "verstak")
  type WindowModel = aspect
  {
    popupToggle: ToggleRef
  }

  #ВнешняяРеализация(модуль = "verstak")
  type WindowSensor = object Sensor
  {
    activeData: Object?

    previousActiveData: Object?

    операция setActiveWindow(window: Object?, debugHint: Text /*(!)= ""*/)
  }
}
