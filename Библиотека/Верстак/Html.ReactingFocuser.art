
import JS = Artel.JavaScript
import Библиотека.Браузер

external
{
  #JS.ВнешняяРеализация(модуль = "verstak")
  operation FocuserReaction(
    key: Text,
    target: HTMLElement,
    model: FocusModel,
    switchEditMode: операция(model: FocusModel?): None? /*(!)= empty*/)
}
