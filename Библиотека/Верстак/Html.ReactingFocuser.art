
import JS = Artel.JavaScript
import Библиотека.HTML

external
{
  #JS.ВнешняяРеализация(модуль = "verstak")
  operation FocuserReaction(
    key: Text,
    target: HTMLElement,
    model: FocusModel,
    switchEditMode: операция(model: FocusModel?): Empty? /*(!)= empty*/)
}
