
import JS = Artel.JavaScript

external
{
  #JS.ВнешняяРеализация(модуль = "verstak")
  operation FocuserReaction(
    key: Text,
    target: HTMLElement,
    model: FocusModel,
    switchEditMode: операция(model: FocusModel?): Nothing? /*(!)= empty*/)
}
