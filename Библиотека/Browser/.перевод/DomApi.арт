псевдонимы(русский)
{
  тип СтилизованныйЭлемент = ElementCSSInlineStyle
  {
    карта-стилей-атрибутов = attributeStyleMap

    стиль = style
  }

  тип ОписаниеСтилей = CSSStyleDeclaration
  {
    цвет-акцента = accent-color

    выравнивание-содержимого = align-content

    выравнивание-элементов = align-items

    выравнивание-самого-себя = align-self

    базис-выраванивания = alignment-baseline

    всё = all

    анимация = animation

    композиция-анимации = animation-composition

    задержка-анимации = animation-delay

    направление-анимации = animation-direction

    длительность-анимации = animation-duration

    режим-заполнения-анимации = animation-fill-mode

    количество-повторений-анимации = animation-iteration-count

    имя-анимации = animation-name

    состояние-анимации = animation-play-state

    функция-тайминга-анимации = animation-timing-function

    внешность = appearance

    соотношение-сторон = aspect-ratio

    фоновый-фильтр = backdrop-filter

    видимость-обратной-стороный = backface-visibility

    фон = background

    вложения-для-фона = background-attachment

    режим-смешивания-фона = background-blend-mode

    отсечение-фона = background-clip

    цвет-фона = background-color

    фоновое-изображение = background-image

    основание-фона = background-origin

    позиция-фона = background-position

    позиция-фона-x = background-position-x

    позиция-фона-y = background-position-y

    повторение-фона = background-repeat

    размер-фона = background-size

    // базовое-смещение = baseline-shift

    // базовый-источник = baseline-source /*(!)*/

    размер-блока = block-size

    рамка = border

    блок-рамки = border-block

    цвет-блока-рамки = border-block-color

    окончание-блока-рамки = border-block-end

    цвет-конца-блока-рамки = border-block-end-color

    стиль-конца-блока-рамки = border-block-end-style

    толщина-конца-блока-рамки = border-block-end-width

    начало-блока-рамки = border-block-start

    цвет-начала-блока-рамки = border-block-start-color

    стиль-начала-блока-рамки = border-block-start-style

    толщина-начала-блока-рамки = border-block-start-width

    стиль-блока-рамки = border-block-style

    толщина-блока-рамки = border-block-width

    рамка-снизу = border-bottom

    цвет-рамки-снизу = border-bottom-color

    радиус-рамки-снизу-слева = border-bottom-left-radius

    радиус-рамки-снизу-справа = border-bottom-right-radius

    стиль-рамки-снизу = border-bottom-style

    толщина-рамки-снизу = border-bottom-width

    свёртка-рамки = border-collapse

    цвет-рамки = border-color

    радиус-окончания-окончания-рамки = border-end-end-radius

    радиус-начала-окончания-рамки = border-end-start-radius

    изображение-рамки = border-image

    начало-изображения-рамки = border-image-outset

    повторение-изображения-рамки = border-image-repeat

    нарезка-изображения-рамки = border-image-slice

    источник-изображения-рамки = border-image-source

    ширина-изоражения-рамки = border-image-width

    рамка-внутри = border-inline

    цвет-рамки-внутри = border-inline-color

    окончание-рамки-внутри = border-inline-end

    конечный-цвет-рамки-внутри = border-inline-end-color

    конечный-стиль-рамки-внутри = border-inline-end-style

    конечная-толщина-рамки-внутри = border-inline-end-width

    начало-рамки-внутри = border-inline-start

    начальный-цвет-рамки-внутри = border-inline-start-color

    начальный-стиль-рамки-внутри = border-inline-start-style

    начальная-толщина-рамки-внутри = border-inline-start-width

    начальный-стиль-рамки-внутри = border-inline-style

    толщина-рамки-внутри = border-inline-width

    рамка-слева = border-left

    цвет-рамки-слева = border-left-color

    стиль-рамки-слева = border-left-style

    толщина-рамки-слева = border-left-width

    радуис-рамки = border-radius

    рамка-справа = border-right

    цвет-рамки-справа = border-right-color

    стиль-рамки-справа = border-right-style

    ширина-рамки-справа = border-right-width

    отступ-рамки = border-spacing

    радиус-окончания-начала-рамки = border-start-end-radius

    радиус-начала-начала-рамки = border-start-start-radius

    стиль-рамки = border-style

    рамка-сверху = border-top

    цвет-рамки-сверху = border-top-color

    радиус-рамки-сверху-слева = border-top-left-radius

    радиус-рамки-сверху-справа = border-top-right-radius

    стиль-рамки-сверху = border-top-style

    толщина-рамки-сверху = border-top-width

    толщина-рамки = border-width

    низ = bottom

    тень-от-контейнера = box-shadow

    расчёт-размеров-контейнер = box-sizing

    разрыв-после = break-after

    разрыв-до = break-before

    разрыв-внутри = break-inside

    сторона-заголовка = caption-side

    цвет-каретки = caret-color

    очистка = clear

    контур-отсечение = clip-path

    правило-отсечения = clip-rule

    цвет = color

    цветовая-интерполяция = color-interpolation

    фильтры-цветовой-интерполяции = color-interpolation-filters

    цветовая-съема = color-scheme

    количество-колонок = column-count

    заполнение-колонок = column-fill

    зазор-колонок = column-gap

    отсечка-колонок = column-rule

    цвет-отсечки-колонок = column-rule-color

    стиль-отсечки-колонок = column-rule-style

    толщина-отсечки-колонок = column-rule-width

    диапазон-колонок = column-span

    ширина-колонки = column-width

    колонки = columns

    содержит = contain

    // contain-intrinsic-block-size

    // contain-intrinsic-height

    // contain-intrinsic-inline-size

    // contain-intrinsic-size

    // contain-intrinsic-width

    контейнер = container

    имя-контейнера = container-name

    тип-контейнера = container-type

    содержимое = content

    // counter-increment

    // counter-reset

    // counter-set

    // css-float

    // css-text

    // cursor

    // cx

    // cy

    // d

    направление = direction

    дисплей = display

    // dominant-baseline

    пустые-ячейки = empty-cells

    заливка = fill

    полупрозрачность-заливки = fill-opacity

    правило-заливки = fill-rule

    фильтр = filter

    // flex

    // flex-basis

    // flex-direction

    // flex-flow

    // flex-grow

    // flex-shrink

    // flex-wrap

    плавающий-элемент = float

    // flood-color

    // flood-opacity

    шрифт = font

    семейство-шрифта = font-family

    // font-feature-settings

    кернинг-шрифта = font-kerning

    // font-optical-sizing

    палитра-шрифта = font-palette

    размер-шрифта = font-size

    // font-size-adjust

    растяжение-шрифта = font-stretch

    стиль-шрифта = font-style

    // font-synthesis

    // font-synthesis-small-caps

    // font-synthesis-style

    // font-synthesis-weight

    вариация-шрифта = font-variant

    // font-variant-alternates

    // font-variant-caps

    // font-variant-east-asian

    // font-variant-ligatures

    // font-variant-numeric

    // font-variant-position

    // font-variation-settings

    // font-weight

    // forced-color-adjust

    зазор = gap

    сетка = grid

    область-сетки = grid-area

    автоматические-колоонки-сетки = grid-auto-columns

    автоматическое-размещение-на-сетке = grid-auto-flow

    автоматические-строки-сетки = grid-auto-rows

    колонка-сетки = grid-column

    конечная-колонка-сетки = grid-column-end

    начальная-колонка-сетки = grid-column-start

    строка-сетки = grid-row

    конечная-строка-сетки = grid-row-end

    начальная-строка-сетки = grid-row-start

    // grid-template

    // grid-template-areas

    // grid-template-columns

    // grid-template-rows

    высота = height

    // hyphenate-character

    перенос-слов = hyphens

    // image-rendering

    // inline-size

    // inset

    // inset-block

    // inset-block-end

    // inset-block-start

    // inset-inline

    // inset-inline-end

    // inset-inline-start

    // isolation

    растяжение-содержимого = justify-content

    растяжение-элементов = justify-items

    растяжение-самого-себя = justify-self

    лево = left

    // guarded
    // length: Number

    // letter-spacing

    // lighting-color

    перенос-строки = line-break

    высота-строки = line-height

    стиль-списка = list-style

    // list-style-image

    // list-style-position

    // list-style-type

    отступ = margin

    // margin-block

    // margin-block-end

    // margin-block-start

    отступ-снизу = margin-bottom

    отступ-внутри = margin-inline

    отступ-окончания-внутри = margin-inline-end

    отступ-начала-внутри = margin-inline-start

    отступ-слева = margin-left

    отступ-справа = margin-right

    отступ-сверху = margin-top

    маркер = marker

    конец-маркера = marker-end

    середина-маркера = marker-mid

    начало-маркера = marker-start

    маска = mask

    отсечение-маски = mask-clip

    композиция-маски = mask-composite

    изображение-маски = mask-image

    режим-маски = mask-mode

    основание-маски = mask-origin

    позиция-маски = mask-position

    повторение-маски = mask-repeat

    размер-маски = mask-size

    тип-маски = mask-type

    глубина-маски = math-depth

    математический-стиль = math-style

    // max-block-size

    максимальная-высота = max-height

    // max-inline-size

    максимальная-ширина = max-width

    // min-block-size

    минимальная-высота = min-height

    // min-inline-size

    минимальная-ширина = min-width

    // mix-blend-mode

    // object-fit

    позиция-объекта = object-position

    смеение = offset

    якорь-смещения = offset-anchor

    расстояние-смещения = offset-distance

    // offset-path

    позиция-смещения = offset-position

    поворот-смещения = offset-rotate

    полупрозрачность = opacity

    порядок = order

    // orphans

    очертание = outline

    цвет-очертания = outline-color

    смещение-очертания = outline-offset

    стиль-очертания = outline-style

    толщина-очертания = outline-width

    переполнение = overflow

    якорь-переполнения = overflow-anchor

    // overflow-clip-margin

    перенос-переполнения = overflow-wrap

    переполнение-x = overflow-x

    переполнение-y = overflow-y

    // overscroll-behavior

    // overscroll-behavior-block

    // overscroll-behavior-inline

    // overscroll-behavior-x

    // overscroll-behavior-y

    отступ-внутри = padding

    // padding-block

    // padding-block-end

    // padding-block-start

    отступ-внутри-снизу = padding-bottom

    // padding-inline

    // padding-inline-end

    // padding-inline-start

    отступ-внутри-слева = padding-left

    отступ-внутри-справа = padding-right

    отступ-внутри-сверху = padding-top

    страница = page

    начало-страницы-после = page-break-after

    начало-страницы-до = page-break-before

    начало-страницы-внутри = page-break-inside

    порядок-рисования = paint-order

    // guarded
    // parent-rule: CSSRule /*| null*/

    перспектива = perspective

    основание-перспективы = perspective-origin

    размещение-содержимого = place-content

    размещение-элементов = place-items

    размещение-самого-себя = place-self

    события-указателя = pointer-events

    позиция = position

    // print-color-adjust

    кавычки = quotes

    // r

    изменение-размера = resize

    право = right

    поворот = rotate

    зазор-строки = row-gap

    // ruby-position

    // rx

    // ry

    масштаб = scale

    // scroll-behavior

    // scroll-margin

    // scroll-margin-block

    // scroll-margin-block-end

    // scroll-margin-block-start

    // scroll-margin-bottom

    // scroll-margin-inline

    // scroll-margin-inline-end

    // scroll-margin-inline-start

    // scroll-margin-left

    // scroll-margin-right

    // scroll-margin-top

    // scroll-padding

    // scroll-padding-block

    // scroll-padding-block-end

    // scroll-padding-block-start

    // scroll-padding-bottom

    // scroll-padding-inline

    // scroll-padding-inline-end

    // scroll-padding-inline-start

    // scroll-padding-left

    // scroll-padding-right

    // scroll-padding-top

    // scroll-snap-align

    // scroll-snap-stop

    // scroll-snap-type

    // scrollbar-color

    // scrollbar-gutter

    // scrollbar-width

    // shape-image-threshold

    // shape-margin

    // shape-outside

    // shape-rendering

    стоп-цвет = stop-color

    стоп-полупрозрачность = stop-opacity

    обводка = stroke

    // stroke-dasharray

    // stroke-dashoffset

    // stroke-linecap

    // stroke-linejoin

    // stroke-miterlimit

    полупрозрачность-обводки = stroke-opacity

    толщина-обводки = stroke-width

    размер-табуляции = tab-size

    табличное-раскладка = table-layout

    выравнивание-текста = text-align

    // text-align-last

    якорь-текста = text-anchor

    // text-combine-upright

    // text-decoration

    // text-decoration-color

    // text-decoration-line

    // text-decoration-skip-ink

    // text-decoration-style

    // text-decoration-thickness

    // text-emphasis

    // text-emphasis-color

    // text-emphasis-position

    // text-emphasis-style

    отступ-текста = text-indent

    ориентация-текста = text-orientation

    переполнение-текста = text-overflow

    рендеринг-текста = text-rendering

    тень-от-текста = text-shadow

    трансформация-текста = text-transform

    смещение-подчеркивания-текста = text-underline-offset

    позиция-подчеркивания-текста = text-underline-position

    перенос-текста = text-wrap

    верх = top

    действие-по-касанию = touch-action

    трансформация = transform

    контейнер-трансформции = transform-box

    основание-трансформации = transform-origin

    стиль-трансформции = transform-style

    переход = transition

    задержка-перехода = transition-delay

    длительность-перехода = transition-duration

    переходное-свойство = transition-property

    функция-тайминга-перехода = transition-timing-function

    перенос = translate

    // unicode-bidi

    выбор-пользователем = user-select

    // vector-effect

    вертикальное-выравнивание = vertical-align

    видимость = visibility

    // webkit-align-content

    // webkit-align-self

    // webkit-line-clamp

    // webkit-text-fill-color

    // webkit-text-stroke

    // webkit-text-stroke-color

    // webkit-text-stroke-width

    пробелы = white-space

    // widows

    ширина = width

    // will-change

    перенос-слов = word-break

    словарный-пробел = word-spacing

    режим-письма = writing-mode

    // x

    // y

    номер-слоя = z-index

    // operation get-property-priority(property)

    // operation get-property-value(property)

    // operation item(index: Number)

    // operation remove-property(property)

    // operation set-property(property, value /*| null*/, priority? = none)
  }
}
