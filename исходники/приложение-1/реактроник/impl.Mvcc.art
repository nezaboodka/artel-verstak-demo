import artel
import artel.js

external
{
  // TransactionalObject

  #ВнешняяРеализация(модуль = "reactronic")
  type TransactionalObject = /*(!) abstract */ object MvccObject
  {
  }

  // ObservableObject

  #ВнешняяРеализация(модуль = "reactronic")
  type ObservableObject = /*(!) abstract */ object MvccObject
  {
  }
}
