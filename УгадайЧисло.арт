
подключить Артель
подключить Браузер
подключить Реактивность
подключить Верстак

выполнить
{
  // РеактивнаяСистема.установить-режим-журнализации(да, ~LoggingLevel.Debug)

  запустить(Окно(
    режим = Режим.автономный,
    тело =
    {
      пусть часы = Часы-Реального-Времени(100)

      о.стиль.цвет = "#f0f0f0"
      о.стиль.цвет-фона = "#1a3043"
      о.стиль.размер-шрифта = "200%"

      Блок(
      {
        о.по-горизонтали = Горизонтально.растяжение
        о.по-вертикали = Вертикально.растяжение
        о.содержимое-по-горизонтали = Горизонтально.по-центру
        о.содержимое-по-вертикали = Вертикально.по-центру

        Блок(
        {
          о.стиль.поле = "1em"
          о.стиль.радиус-границы = "0.5em"
          о.стиль.цвет-фона = "rgba(0, 0, 0, 0.5)"
          // о.содержимое-по-горизонтали = Горизонтально.по-центру

          Блок(
            режим = Режим.автономный,
            тело =
            {
              Блок({ о.текст = "Угадай число" })

              с-новой-строки()

              пусть ч = часы.час.в-текст(2, "0")
              пусть м = часы.минута.в-текст(2, "0")
              пусть с = часы.секунда.в-текст(2, "0")
              Блок({ о.текст = "{ч}:{м}:{с}" })
            })

          с-новой-строки()

          Таблица(
          {
            о.стиль.зазор-сверху = "1em"
            о.по-горизонтали = Горизонтально.растяжение
            о.содержимое-по-горизонтали = Горизонтально.по-центру

            Кнопка("1")
            Кнопка("2")
            Кнопка("3")

            с-новой-строки()

            Кнопка("4")
            Кнопка("5")
            Кнопка("6")

            с-новой-строки()

            Кнопка("7")
            Кнопка("8")
            Кнопка("9")

            с-новой-строки()

            Кнопка("")
            Кнопка("0")
            Кнопка("")
          })
        })
      })
  }))
}

функция Кнопка(надпись: Текст)
{
  Блок(
  {
    о.стиль.зазор = "0.25em"
    о.стиль.поле = "0.25em"
    о.стиль.цвет-границы = "rgba(255, 255, 255, 0.75)"
    о.стиль.толщина-границы = "1pt"
    о.стиль.стиль-границы = "solid"
    о.стиль.радиус-границы = "0.2em"

    Блок({ о.текст = надпись })
  })
}
