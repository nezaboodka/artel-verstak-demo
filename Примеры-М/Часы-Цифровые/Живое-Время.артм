модули
  Реактивность
  Математика
  JS = Platform.JavaScript
всё

тип Живое-Время
  перем интервал: Целое

  // #сигнал
  перем год: Целое = 0

  // #сигнал
  перем месяц: Целое = 0

  // #сигнал
  перем день: Целое = 0

  // #сигнал
  перем час: Целое = 0

  // #сигнал
  перем минута: Целое = 0

  // #сигнал
  перем секунда: Целое =  0

  // #сигнал
  перем миллисекунда: Целое = 0

  операция создание(интервал: Целое = 100)
    экз.интервал = интервал
    такт()
  всё

  // Скрыто

  // #транзакция
  операция такт()
    перем с = Дата-Время-Местное.сейчас()
    год = с.год
    месяц = с.месяц
    день = с.день
    час = с.час
    минута = с.минута
    секунда = с.секунда
    миллисекунда = с.миллисекунда
    // Взводим таймер на сам же метод "такт"
    перем калибровка: Целое = остаток(с.миллисекунда, экз.интервал)
    JS.set-timeout(такт, экз.интервал - калибровка)
  всё
всё
