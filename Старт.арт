подключить {
  Реактивность
  Верстак
}

выполнить {
  // Веб-Драйвер<Любое>.эффекта-мерцания = да
  // Реактивная-Система.установить-режим-журнализации(да, Уровень-Журнализации.Debug)

  запустить({

    // перем время = Живое-Время(интервал = 11) // миллисекунд

    // Окно-Приложения({
    //   стиль.цвет = "#f0f0f0"
    //   стиль.цвет-фона = "#091F27"
    //   стиль.семейство-шрифтов = "Monospace"
    //   стиль.размер-шрифта = "3vh"
    //   стиль.курсор = "default"

    //   Часы-Цифровые(время)
    // })

    перем состояние-игры = Состояние-Игры-Угадай-Число()

    Окно-Приложения({
      стиль.цвет = "#f0f0f0"
      стиль.цвет-фона = "#091F27"
      стиль.семейство-шрифтов = "Monospace"
      стиль.размер-шрифта = "3vh"
      стиль.курсор = "default"

      Угадай-Число(состояние-игры)
    })
  }, [])
}
